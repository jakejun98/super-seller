# .gitignore

# 1. 의존성 및 빌드 파일 (용량 큼)
node_modules/
.next/
build/
dist/

# 2. 환경 변수 (보안 중요! 절대 올리지 말 것)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# 3. OS 및 에디터 설정 파일 (잡동사니)
.DS_Store
.vscode/
*.log